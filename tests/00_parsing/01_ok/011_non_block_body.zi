foo()
  return;

bar(b:bool): int
  if b return 42
  else return 42

baz(b:bool)
  while b { };

quux(r:{x:int})
  r.x = 42

abcdef(): int
  h:bool = false

bbbb()
  array:int[42]

loop() loop()

//@PRACOWNIA
//@stop_after parser
