test()
{
    x = a or b and c;
    x = a and b or c and d;
    x = a or b or c;
    x = a < b < c;
    x = a < b == c < d;
    x = a < b * c == c + d < e * f
    x = a * b / d % e

}

//@PRACOWNIA
//@stop_after parser
