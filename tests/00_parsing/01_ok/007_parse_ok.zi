f()
{
    return "Wroclaw"
}

f()
{
    return {5, 10, 15, "zaraz sie zacznie"}
}

f()
{
    x:int x = 10 y = 17
}

f()
{
    x = 1 + 2 + 3
    x = 1 - 2 - 3
    x = 1 * 2 * 3
    x = 1 / 2 / 3
    x = 1 % 2 % 3
    x = 1 and 2 and 3
    x = 1 or 2 or 3
}


f()
{
    if (x > 10) y = 1
    if x > 10 y = 1
    if x > 10 {
        return "42"
    }
    if pred() y = 1
    if pred() and y or z x = 1
    z = not (a and z)
}

f()
{
    if (x > 10) y = 1 else z = 1
    if x > 10 y = 1 else z = 1
    if x > 10 { 
        return "42"
    } else z = 1

    if pred() y = 1 else {
        z = 1
    }
    if pred() and y or z x = 1 else {
        z = 3
    }
}

f()
{
    if x if y z = 1 else z = 2
}

f()
{
    while (x > 10) y = 1
    while x > 10 y = 1
    while x y = 1
    if true {
        z = bar
        return;
        x = 123
    }
    while pred() y = 1
    while pred() {
        zmienna = wartosc
        return;
    }
}

f()
{
    g()
    g(1, 2)
    g(1, 2, 3)
}


f()
{
    z = length("x")
    z = length(x + y)
    z = length(x - y)
    z = length(x and y)
    z = length(x / y)
    z = length({1,2,3} + 1)
}

g()
{
    z = 'a'
    z = 'b'
    z = '0'
    z = '\0'
    z = '\t'
    z = '\n'
    z = '\r'
    z = '\"'
    z = '\''
    z = '''
    z = '\\'
}

g()
{
    z = "a"
    z = "abece de"
    z = "abece\nde"
    z = "abece\\de"
    z = "abece\"de"
}

g(x:int[][]):bool[][] {

}

g(x:int[][]):bool[][] {

}

g()
{
    x:int[] = {}
    x:int[][][]
    x:int[][][1]
    x:int[][foo][1]
    x:int[][n][3 + 5 * 6]
}

g()
{
    x[0] = 1
    x[0][1] = 2
    x[a][b] = c[d]
}

//@PRACOWNIA
//@stop_after parser
